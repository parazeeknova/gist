{
  "name": "@gist/backy",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "go run main.go",
    "build": "go build -o bin/backy",
    "start": "./bin/backy",
    "check-types": "go vet ./...",
    "check": "$HOME/go/bin/gofumpt -w . && $HOME/go/bin/goimports -w . && $HOME/go/bin/golangci-lint run"
  }
}
